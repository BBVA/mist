# Name: check
# Version: 1.0.0
# Description: executes a branch or another based on check result using 'is' operator
# Tags: command, check

r = exec ("/bin/bash -c {%param}")
check r.result is Success {
  print("FOO")
} else {
  print("BAR")
}
