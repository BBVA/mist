# Name: exec
# Version: 1.0.0
# Description: executes a custom user shell command
# Tags: command, exec, shell
exec ("bash -c 'ls --sadfasdf'") => r {
    check r.result is Error {
        abort
    }
}
