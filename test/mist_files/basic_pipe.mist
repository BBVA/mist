function producer (i) {
    print "Producer" i
    send i
}

function consumer(i, received) {
    #sleep(i)
    #exec("sleep {i}")
    print "Consumer" i ":" received
}

producer(1) => numbers
consumer(1 :numbers)
producer(2) => numbers
producer(3) => numbers
consumer(2 :numbers)
producer(4) => numbers
producer(5) => numbers
producer(6) => numbers
