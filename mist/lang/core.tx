Program:
  commands*=Command
;

Command:
  BuiltIn | SaveCommand | DataCommand | CheckCommand | IterateCommand | ##MODULES##
;

DataCommand:
  'data' name=ID '{'
    params*=ID
  '}'
;

IDorSTRING:
  data=/(?ms)\'{3}(.*?)\'{3}/ | id=ID(('.'child=ID))? | string=STRING
;

SaveCommand:
  'put' sources*=IDorSTRING '=>' target=ID ('(' params*=ID ')')?
;

CheckCommand:
  'check' var=ID 'is' result=ID '{'
    commands*=Command
  '}'
;

IterateCommand:
  'iterate' var=ID '=>' name=ID '{'
    commands*=Command
  '}'
;

Comment:
  /#.*$/
;
