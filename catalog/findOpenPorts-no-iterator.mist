# Name: findOpenPorts
# Description: Read file with list of IPs and check for open ports, write the result in csv file
# Tags: nmap, network, ports

CSVput "catalog/myhosts.csv" => myHosts

findOpenPorts {
    input {
        ip <= "127.0.0.1"
        ports <= "80,5432,1000,9000"
    }
    output {
        result
        openPorts
        console
    }
    then {
        print openPorts
    }
}
