function producer (i) => result {
    print "Producer" i  
    send i
}

function consumer(i) {
    print "Consumer" i ":" received
}

producer(1) to numbers

consumer(1) from numbers

producer(2) to numbers
producer(3) to numbers

consumer(2) from numbers

producer(4) to numbers
producer(5) to numbers
producer(6) to numbers
