function tee(p) {
    print(p)
    send(p)
}

# Test 1
# "hola" => tee("foo") => explicitQueue

# Test 2
# send("hola", "q")
# :q => tee() => explicitQueue

# Test 3
send("hola", "q")
q => tee() => explicitQueue

#"hola" => tee("foo") => explicitQueue => tee("bar")

#"hola" => tee("foo") => tee("bar")